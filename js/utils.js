


/** Object that contains our utility functions.
  * Attached to the window object which acts as a global namespace.
  */
 window.utils = {};

/** Randomizes the content of an array for different purposes
  * @return {number}
  */
window.utils.randOrd=function (){
    
         
    return (Math.round(Math.random())- 0.5);
       
   };